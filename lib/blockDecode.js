import blockType from './blockType';
import BlockState from './BlockState';

function blockDecode (adapter) {
  let blocks = adapter.getBlocks();

  return blocks.map(function (block) {

  })
}

export default blockDecode;
